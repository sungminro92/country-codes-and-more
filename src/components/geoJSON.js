import geoCoordinates from "../geoCoordinates"

const apiKey = String(process.env.REACT_APP_MY_API_KEY)

const geoJSON = () => {
    return (
        <div>geoJSON map</div>
    )
}


export default geoJSON